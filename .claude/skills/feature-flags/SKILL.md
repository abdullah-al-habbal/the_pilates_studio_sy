---
name: feature-flags
description: Feature flag strategy via laravel/pennant for freemium gating. Use when gating multi-branch or job-board features behind subscription tiers.
---

# Feature flags

`laravel/pennant` is declared in `composer.json` but not yet wired into application code.

Intended for freemium gating (multi-branch, job board features). Reference Pennant documentation for `Feature::active()` usage in handlers and actions.
